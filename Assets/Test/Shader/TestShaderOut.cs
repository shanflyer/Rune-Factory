using UnityEngine;

public class TestShaderOut : MonoBehaviour
{
    public ComputeShader computeShader;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
